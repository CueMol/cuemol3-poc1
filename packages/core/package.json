{
  "name": "@cuemol/core",
  "version": "1.0.0",
  "description": "cuemol core package",
  "main": "lib/index.js",
  "scripts": {
    "install": "cmake-js build --directory ../.. --out ./build && webpack",
    "build": "cmake-js build --directory ../.. --out ./build && webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "lib",
    "build/Release"
  ],
  "dependencies": {
    "bindings": "^1.5.0",
    "cmake-js": "^6.3.0"
  },
  "devDependencies": {
    "node-addon-api": "^4.2.0"
  },
  "cmake-js": {
    "runtime": "electron",
    "runtimeVersion": "16.0.6",
    "arch": "x64"
  }
}
